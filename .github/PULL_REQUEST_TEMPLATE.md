# What does this PR resolve? 🚀

<!-- One-line summary. Then bullets: the key changes and, briefly, the why. -->

# Details 📝

<!-- Implementation specifics, trade-offs, anything a reviewer needs.
     This app handles live funded private keys - call out anything that
     affects where keys travel, what gets printed, or what leaves the browser. -->

# Checklist ✅

- [ ] Merged latest `main` and resolved conflicts
- [ ] `pnpm test` passes
- [ ] `pnpm build` passes (`tsc -b` type-checks tests too)
- [ ] `pnpm lint` introduces no new errors
- [ ] Changes are covered by tests or manually verified
- [ ] No private key is logged, printed as text, or sent off-origin
- [ ] Docs or README updated where relevant
- [ ] Self-reviewed the diff
